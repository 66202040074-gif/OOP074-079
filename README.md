# 🔥 Calorie Calculator - OOP Project

โปรเจกต์ Calorie Calculator เป็นแอปพลิเคชันสำหรับคำนวณแคลอรี่แบบ Object-Oriented Programming (OOP) ด้วยภาษา Python

## 📋 คุณสมบัติ

### 1. **คำนวณข้อมูลสุขภาพ**
   - คำนวณ BMI (Body Mass Index)
   - จำแนกสถานะน้ำหนัก (ต่ำกว่าเกณฑ์, ปกติ, ท้องอ้วน, อ้วนมาก)

### 2. **คำนวณแคลลอรี่**
   - คำนวณ BMR (Basal Metabolic Rate) โดยใช้สูตร Mifflin-St Jeor
   - คำนวณ TDEE (Total Daily Energy Expenditure)
   - หารเป้าหมายแคลลอรี่ (ลดน้ำหนัก, รักษา, เพิ่มน้ำหนัก)

### 3. **บันทึกกิจกรรม**
   - เลือกจากกิจกรรม 12+ ประเภท
   - บันทึกแคลลอรี่ที่ใช้ในแต่ละกิจกรรม
   - ดูสรุปแคลลอรี่ทั้งหมด

### 4. **Web Interface**
   - UI ที่สวยงามและใช้งานง่าย
   - Responsive design (ใช้งานได้บนมือถือและคอมพิวเตอร์)

## 🏗️ โครงสร้าง OOP

### คลาส (Classes)

#### 1. **Gender (Enum)**
ระบุเพศ: ชาย (MALE) หรือหญิง (FEMALE)

#### 2. **Activity (Enum)**
ระดับการออกกำลัง:
- `SEDENTARY` (1.2) - นั่งประจำ
- `LIGHTLY_ACTIVE` (1.375) - ออกกำลังน้อย
- `MODERATELY_ACTIVE` (1.55) - ออกกำลังปานกลาง
- `VERY_ACTIVE` (1.725) - ออกกำลังมาก
- `EXTREMELY_ACTIVE` (1.9) - ออกกำลังหนัก

#### 3. **Person**
เก็บข้อมูลบุคคล

**Attributes:**
- `name`: ชื่อ
- `age`: อายุ
- `weight_kg`: น้ำหนัก (กิโลกรัม)
- `height_cm`: ส่วนสูง (เซนติเมตร)
- `gender`: เพศ
- `created_date`: วันที่สร้าง

**Methods:**
- `get_bmi()`: คำนวณ BMI
- `get_bmi_category()`: จำแนกหมวดหมู่น้ำหนัก
- `get_info()`: ส่งข้อมูลทั้งหมด

#### 4. **CalorieCalculator**
คำนวณแคลลอรี่

**Attributes:**
- `person`: อ็อบเจกต์ Person
- `ACTIVITIES`: ตารางแคลลอรี่ของกิจกรรม

**Methods:**
- `calculate_bmr()`: คำนวณ BMR (Mifflin-St Jeor formula)
- `calculate_tdee(activity_level)`: คำนวณ TDEE
- `calculate_activity_calories(activity_name, minutes)`: คำนวณแคลลอรี่จากกิจกรรม
- `get_all_activities()`: ส่งรายชื่อกิจกรรม
- `get_calorie_info(activity_level)`: ส่งข้อมูลแคลลอรี่ทั้งหมด

#### 5. **CalorieTracker**
บันทึกกิจกรรมและแคลลอรี่

**Methods:**
- `add_activity(activity_data)`: เพิ่มกิจกรรม
- `get_total_calories()`: รวมแคลลอรี่ทั้งหมด
- `get_activity_logs()`: ส่งบันทึกทั้งหมด
- `clear_logs()`: ล้างบันทึก

## 📁 ไฟล์ของโปรเจกต์

```
OOP 074,079/
├── bmi.py              # Python OOP Classes
├── index.html          # Web Interface (HTML + CSS + JavaScript)
├── main.py             # Flask Application (Optional)
└── README.md           # Documentation
```

## 🚀 วิธีใช้งาน

### แบบ 1: ใช้ HTML Interface โดยตรง
1. เปิดไฟล์ `index.html` ในเบราว์เซอร์
2. กรอกข้อมูลบุคคล
3. คำนวณ BMI และแคลลอรี่
4. บันทึกกิจกรรม

### แบบ 2: ใช้ Flask Web Server (ต้องติดตั้ง Flask)
```bash
# ติดตั้ง Flask
pip install flask

# เรียกใช้โปรแกรม
python main.py

# เปิดเบราว์เซอร์ที่ http://localhost:5000
```

### แบบ 3: ใช้ Python Script โดยตรง
```bash
python bmi.py
```

## 📊 ตัวอย่างการใช้งาน (Python)

```python
from bmi import Person, Gender, Activity, CalorieCalculator, CalorieTracker

# สร้างข้อมูลบุคคล
person = Person("สมชาย", 30, 75, 175, Gender.MALE)

# คำนวณ BMI
print(f"BMI: {person.get_bmi()}")
print(f"สถานะ: {person.get_bmi_category()}")

# คำนวณแคลลอรี่
calc = CalorieCalculator(person)
print(f"BMR: {calc.calculate_bmr()} แคลลอรี่/วัน")
print(f"TDEE: {calc.calculate_tdee(Activity.MODERATELY_ACTIVE)} แคลลอรี่/วัน")

# บันทึกกิจกรรม
tracker = CalorieTracker()
activity = calc.calculate_activity_calories("วิ่ง", 30)
tracker.add_activity(activity)
print(f"แคลลอรี่ทั้งหมด: {tracker.get_total_calories()}")
```

## 📐 สูตรคำนวณ

### BMR (Basal Metabolic Rate) - Mifflin-St Jeor Formula

**ชาย:**
```
BMR = (10 × W) + (6.25 × H) - (5 × A) + 5
```

**หญิง:**
```
BMR = (10 × W) + (6.25 × H) - (5 × A) - 161
```

โดยที่:
- W = น้ำหนัก (กิโลกรัม)
- H = ส่วนสูง (เซนติเมตร)
- A = อายุ (ปี)

### TDEE (Total Daily Energy Expenditure)
```
TDEE = BMR × Activity Factor
```

### BMI (Body Mass Index)
```
BMI = Weight (kg) / Height² (m²)
```

### ลดน้ำหนัก
```
Daily Calorie Goal = TDEE - 500
```
(ลด 500 แคลลอรี่ต่อวัน = ลด ~0.5 กิโลกรัมต่อสัปดาห์)

## 🎯 เป้าหมายโปรเจกต์

- ✅ ประยุกต์ใช้ OOP Principles (Encapsulation, Inheritance, Polymorphism)
- ✅ สร้าง Class และ Method ที่มีความหมาย
- ✅ ใช้ Enum สำหรับค่าคงที่
- ✅ สร้าง Web Interface ให้สะดวก
- ✅ คำนวณแคลลอรี่อย่างถูกต้อง

## 🎓 หัวข้อ OOP ที่ใช้

1. **Encapsulation** - ซ่อนข้อมูลภายในคลาส
2. **Abstraction** - ใช้ Enum สำหรับ Gender และ Activity
3. **Class Design** - ออกแบบคลาสให้มีความหมาย
4. **Method Organization** - จัดเรียง Method อย่างเหมาะสม
5. **Data Types** - ใช้ Type Hints เพื่อความชัดเจน

## 💡 ความเห็น

โปรเจกต์นี้เป็นตัวอย่างที่ดีของการออกแบบ OOP ที่ปฏิบัติได้จริง โดยผสมผสาน:
- Backend (Python OOP)
- Frontend (HTML/CSS/JavaScript)
- Business Logic (คำนวณแคลลอรี่)

---

📝 **สร้างโดย:** GitHub Copilot  
📅 **วันที่:** 2026-02-26  
🏷️ **เวอร์ชัน:** 1.0
